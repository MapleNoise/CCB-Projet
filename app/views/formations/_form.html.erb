<%= form_for(@formation, html: {class: "formulaire--back"}) do |f| %>
  <% if @formation.errors.any? %>
    <div class="error_explanation">
      <h2><%= pluralize(@formation.errors.count, "error") %> prohibited this formation from being saved:</h2>

      <ul>
      <% @formation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field field--back">
    <span class="field__label"><%= f.label :ref %></span>
    <%= f.text_field(:ref, class: "field__control")%>
  </div>
  <div class="field field--back">
    <span class="field__label"><%= f.label :nom %></span>
    <%= f.text_field(:nom, class: "field__control")%>
  </div>
  <div class="field field--back">
    <span class="field__label"><%= f.label :descriptionCourte %></span>
    <%= f.text_field(:descriptionCourte, class: "field__control") %>
  </div>
  <div class="field field--back">
    <span class="field__label"><%= f.label :description %></span>
    <%= f.text_field(:description, class: "field__control") %>
  </div>
  <div class="field field--checkbox field--back">
    <span class="field__label"><%= f.label :estPublic %></span>
    <%= f.check_box(:estPublic, class: "field__control") %>
  </div>
  <div class="field field--back">
    <div class="actions">
      <%= f.submit class: "button button--submit"%>
    </div>
  </div>
<% end %>
