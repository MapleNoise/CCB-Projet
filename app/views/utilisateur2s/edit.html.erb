<% provide :titreH1, "Éditer un profil utilisateur"%>

<%= form_for(@utilisateur2, html: {class: "formulaire--back"}) do |f| %>
  <% if @utilisateur2.errors.any? %>
    <div class="error_explanation">
      <h2><%= pluralize(@utilisateur2.errors.count, "error") %> prohibited this utilisateur2 from being saved:</h2>

      <ul>
      <% @utilisateur2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="field field--back">
  <span class="field__label"><%= f.label "Nom" %></span>
    <%= f.text_field :nom, :class=>'field__control' %>
  </div>

  <div class="field field--back">
    <span class="field__label"><%= f.label "Prénom"  %></span>
    <%= f.text_field :prenom, :class=>'field__control' %>
  </div>

<% if(Utilisateur2.find_by(id: session[:user_id]).isAdmin?) %>
    <div class="field field--back">
   <span class="field__label">Fonction</span>
  <%= select_tag(:fonctions, options_from_collection_for_select(Fonction.all, :id, :nom, @utilisateur2.fonctionId), :class=>'field__control field__control--select') %>
</div>  
  
<% end %>

<div class="field field--back">

  <div class="actions">
    <%= f.submit 'Mettre à jour', :class=>'button button--submit' %>
  </div>
</div>
<% end %>

<% if(Utilisateur2.find_by(id: session[:user_id]).isAdmin?) %>
	<div class="field">
  		<%= link_to 'Afficher', @utilisateur2 , class: "button button--link"%>
	</div>
<!--|<%= link_to 'Back', utilisateur2s_path %>-->

<% end %>
