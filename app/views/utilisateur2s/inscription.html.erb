<%= form_for(@utilisateur2, html: {class: "formulaire--back"}) do |f| %>
  <% if @utilisateur2.errors.any? %>
    <div class="error_explanation">
      <h2><%= pluralize(@utilisateur2.errors.count, "error") %> prohibited this utilisateur2 from being saved:</h2>

      <ul>
      <% @utilisateur2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field field--back">
    <span class="field__label"><%= f.label "Nom" %></span>
    <%= f.text_field( :nom, class: "field__control") %>
  </div>

  <div class="field field--back">
    <span class="field__label"><%= f.label "Prenom" %></span>
    <%= f.text_field( :prenom, class: "field__control") %>
  </div>

  <div class="field field--back">
    <span class="field__label"><%= f.label :email %></span>
    <%= f.text_field( :email, class: "field__control") %>
  </div>
  
  <div class="field field--back">
    <span class="field__label"><%= f.label "Confirmation email" %></span>
    <%= f.text_field( :email_confirmation, class: "field__control") %>
  </div>

  <div class="field field--back">
    <span class="field__label"><%= f.label "Mot de passe" %></span>
    <%= f.password_field( :password, class: "field__control") %>
  </div>
  
  <div class="field field--back">
    <span class="field__label"><%= f.label "Confirmation mot de passe" %></span>
    <%= f.password_field( :password_confirmation, class: "field__control") %>
  </div>

  <div class="field field--back">  
  <%= hidden_field_tag :fonctions, 3 %>
  </div>
  
  <div class="field field--back">
    <div class="actions">
      <%= f.submit  class: "button button--submit"%>
    </div>
  </div>
  
<% end %>
