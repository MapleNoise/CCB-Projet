<%= form_for(@fonction, html: {class: "formulaire--back"}) do |f| %>
  <% if @fonction.errors.any? %>
    <div class="error_explanation">
      <h2><%= pluralize(@fonction.errors.count, "error") %> prohibited this fonction from being saved:</h2>

      <ul>
      <% @fonction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field field--back">
    <span class="field__label"><%= f.label :nom %></span>
    <%= f.text_field( :nom, class: "field__control") %>
  </div>
<div class="field field--back">
  <div class="actions">
    <%= f.submit class: "button button--submit"%>
  </div>
</div>
<% end %>
