<%= form_for(@tag, html: {class: "formulaire--back"}) do |f| %>
  <% if @tag.errors.any? %>
    <div class="error_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field field--back">
    <span class="field__label"><%= f.label :nom %></span>
    <%= f.text_field( :nom, class: "field__control") %>
  </div>
  <div class="field field--back">
    <span class="field__label"><%= f.label :mere %></span>
    <%= f.text_field( :mere, class: "field__control") %>
  </div>
<div class="field field--back">
  <div class="actions">
    <%= f.submit class: "button button--submit" %>
  </div>
</div>
<% end %>
